{
  "name": "@komara/coding-agent",
  "version": "1.0.0",
  "description": "Core agent runtime and message router for Komara Agency",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "pytest test/ -v"
  },
  "dependencies": {
    "@komara/ai": "1.0.0"
  }
}
